import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'calculadora20202005863',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
